export default {
  expo: {
    name: 'BidDarbaarApp',
    slug: 'BidDarbaarApp',
    plugins: ['expo-image-picker'], // Add the expo-image-picker plugin here
    // other configurations if needed
  },
};
